kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: {{ .Values.global.compositionName }}-composition-events-panel
  namespace: {{ .Values.global.compositionNamespace }}
spec:
  widgetData:
    actions: {}
    items:
      - resourceRefId: composition-events-panel-eventlist
  resourcesRefs:
    items:
      - id: composition-events-panel-eventlist
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: {{ .Values.global.compositionName }}-composition-events-panel-eventlist
        namespace: {{ .Values.global.compositionNamespace }}
        resource: eventlists
        verb: GET